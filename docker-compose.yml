services:
  nltype:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - nltype-data:/app/data
    environment:
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
    restart: unless-stopped

volumes:
  nltype-data:
